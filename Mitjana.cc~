#include <iostream>
using namespace std;

int main() {
  
  
cout.setf(ios::fixed);
cout.precision (2);

  
  double x, s, b, m;
  s=0;
  b=0;
  
  while (cin >> x) {
    s=s+x;
    b=b+1;
  }
  m=s/b;
  cout << m << endl;
}